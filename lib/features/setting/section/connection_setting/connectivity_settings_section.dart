import 'package:flutter/widgets.dart';

class ConnectivitySettingsSection extends StatelessWidget {
  const ConnectivitySettingsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
